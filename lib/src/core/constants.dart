class K {
  static const double gyroThreshold = 0.7;
  static const double crossAxisFactor = 0.5;
  static const Duration stageTimeout = Duration(microseconds: 800);
  static const Duration doubleTapWindow = Duration(seconds: 3);
  static const Duration minSampleInterval = Duration(milliseconds: 20);
}
